# Verification Archive

This directory stores historical local verification runs and screenshots.

- Keep only timestamped folders (example: `2026-02-18/`).
- Keep canonical latest artifacts in `docs/verification/` root:
  - `flow-latest.json`
  - `event-latest.json`
  - `STATUS.md`
